/Manifest*.toml
/LocalPreferences.toml
/test/options_explorer.jl

# sbatch launcher for options_explorer.jl -- cluster-only (SLURM), same
# reasoning as the benchmark tooling below.
/test/run_options_explorer.sh
/test/options_explorer_logs/

# Section 3.3 seasonal-cycle reproduction (365-day, hourly-dt run): cheap
# enough per step, but the full annual cycle plus mp4 rendering is more than
# worth doing on the cluster instead of the laptop -- kept off every machine
# that isn't the cluster rather than tracked in git, same as the benchmark
# tooling below.
/test/reproduce_section_3_3.jl

# Cluster-only benchmark tooling for the solver x thread-count sweep (task 4):
# expensive to run, only ever meant to execute on albedo0 via sbatch -- kept
# off every machine that isn't the cluster rather than tracked in git.
/test/benchmark_solver.jl
/test/benchmark_array.sh
/test/plot_benchmark.jl
/test/plot_cpu_vs_gpu.jl
/test/gpu_diagnostic.jl
/test/benchmark_results/
/test/benchmark_logs/

# OS junk
.DS_Store
Thumbs.db

# Julia build/test artifacts
*.jl.cov
*.jl.*.cov
*.jl.mem
/docs/build/

# Generated example output
/test/Figures/
